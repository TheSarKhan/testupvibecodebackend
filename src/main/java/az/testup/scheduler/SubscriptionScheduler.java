package az.testup.scheduler;

import org.springframework.stereotype.Component;

@Component
public class SubscriptionScheduler {
    // Pending downgrade logic removed — all plan changes are immediate
}
